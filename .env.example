# Copie para .env e ajuste conforme necessário.
POSTGRES_DB=factcheck
POSTGRES_USER=factcheck
POSTGRES_PASSWORD=factcheck

# Horário da coleta diária (HH:MM). O container usa UTC por padrão.
CRAWL_TIME=03:00

# Executa uma coleta logo ao subir o container (true/false).
RUN_ON_START=true
